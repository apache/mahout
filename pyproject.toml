[tool.poetry]
name = "qumat"
version = "0.0.1"
description = "A library for composing quantum machine learning."
authors = ["Apache Mahout"]
license = "ASFv2"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
qiskit = "^1.3.2"
qiskit-aer = "^0.16.0"
cirq = "^1.4.0.dev20240509162830"
amazon-braket-sdk = "^1.88.3"
sympy = "^1.13.2"

[tool.poetry.dev-dependencies]
pytest = "^8.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
